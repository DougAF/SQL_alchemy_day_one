import os
from sqlalchemy import create_engine

db_path = os.path.join('..', 'Resources', 'Census_Data.sqlite')  # Path to sqlite

# Create engine to establish connection

# Try out some basic queries